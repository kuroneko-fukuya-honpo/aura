---
id: changelog
title: 更新履歴
slug: /changelog
sidebar_position: 70
---

# 更新履歴

AURA（Article Update & Reading Assistant）

最終更新日: 2026年9月23日

---

区分は次の4つです。

- **機能追加** — 新しくできるようになったこと
- **改善** — もとからあったものを使いやすくしたこと
- **不具合修正** — 直したこと
- **対応** — OS や外部の決まりに合わせたこと

**お使いのバージョンより上を読めば、更新で何が変わるかが分かります。**
お使いのバージョンは、アプリの 設定 →「その他」→「AURA について」でご確認いただけます
（いまは `バージョン 1.0.0` と表示され、うしろの枝番は出ていません）。

見出しの `33-38` は、**その更新で入ったバージョンの範囲**です。33 から 38 までの変更が、
まとめて1回の更新で届きます。

各行の末尾の `(1.0.0-33)` は、**その変更が入ったバージョン**です。
**その番号以降でお使いいただけます。** 1つの機能が何回かに分けて作られたときは、
**出来上がった時点の番号**を書いています。

いちばん上の **次回配信予定** は、**まだお届けしていない**変更です。

**`1.0.0-32` までは以前の書き方です。** 1つの変更に1つの番号を割り当てていないため、
行末の番号は付けていません。見出しのブロックが、その版までにまとめて入った変更を示します。
1つの変更につき1つ番号を進める書き方は **`1.0.0-33` から**です。

---

## 次回配信予定

<div className="paste">・(改善)設定 →「その他」→「AURA について」で、バージョンの枝番まで表示するようにしました。変更履歴の番号と照らし合わせられます(1.0.0-33)
・(改善)アプリのアイコンを新しい絵に変更しました(1.0.0-36)
・(不具合修正)開いていない記事が既読になることがあったのを直しました。更新で新着が一覧の上に入ったときや、横に並べる表示（折りたたみを開いた画面など）で新着を選んだとき、削除済みの記事をウィジェットから開いたときに起きていました(1.0.0-38)
・(改善)購読を追加すると、その購読の記事がすぐ一覧に出て、全文の先読みもその購読の記事から始まるようにしました。これまでは次の更新まで待つことがありました(1.0.0-39)
・(改善)購読リストの管理で購読を押したときのフォルダの選択に「＋ 新しいフォルダ」を足しました。名前を入れるとその場でフォルダを作って入れます。同じ名前のフォルダがあれば、そちらに入れます(1.0.0-40)
・(機能追加)記事の本文の画像を押すと、大きく表示するようにしました。2 本指で拡大でき、戻るキーか画像の外を押すと記事に戻ります(1.0.0-41)
・(不具合修正)一覧をスクロールしただけで記事が既読になることがあった問題を修正しました。とくに購読を追加した直後など、一覧に記事がまとめて入ったときに、見ていない記事が一度に既読になることがありました(1.0.0-42)
・(改善)これにともない「すぐに既読にする」の設定をなくし、既読になるのは記事を開いたときだけになりました。まとめて片付けたいときは「全既読」をお使いください(1.0.0-42)
・(改善)眼鏡（Even G2）のフォルダ一覧に、最終更新日時が出るようになりました。未読 / すべて / 保存 を切り替えると、その範囲でいちばん新しい記事の日時に変わります(1.0.0-43)
・(機能追加)note の記事を共有すると、その著者の投稿を購読できるようになりました。note アプリやブラウザの「共有」から AURA を選ぶと、著者の確認とフォルダの選択のあと、すぐに記事が並びます(1.0.0-44)
・(機能追加)note 以外のサイトやブログも、記事を共有するだけで購読できるようになりました。ページに RSS の案内が無いときはユーザーのページやサイトのトップページ、よくある場所も探し、候補が複数あるときは選べます(1.0.0-45)
・(機能追加)特定の言葉を含む記事を隠したり、上に出したりできるようになりました（AURA Plus）(1.0.0-46)
・(機能追加)読み終えた記事を、一定の期間が過ぎたら自動で片付けられるようになりました（AURA Plus）(1.0.0-47)
・(機能追加)YouTube の動画やチャンネルを共有すると、そのチャンネルの新着動画を購読できるようになりました(1.0.0-48)
・(改善)キーワード購読・購読ディレクトリからの一括追加・Even G2 連携・ウィジェットは AURA Plus の機能になりました(1.0.0-49)
・(改善)キャッシュの容量の上限と、OPML から取り込むキーワード購読も AURA Plus の機能になりました(1.0.0-50)
・(対応)記事の一覧に広告が出るようになりました。AURA Plus では出ません(1.0.0-51)
・(改善)一覧の広告の間隔を調整しました(1.0.0-53)
・(不具合修正)YouTube のチャンネルの購読で「配信元が見つかりません（HTTP 404）」と出て更新できないことがあった問題を修正しました。YouTube 側が一時的に応答しないときに起きていました。取り直すようにし、それでも取れないときは一時的な不調として表示します(1.0.0-54)
・(不具合修正)YouTube のチャンネルや動画を共有しても購読できなくなっていた問題を修正しました(1.0.0-55)
・(改善)購読の管理からフォルダの並び順を変えられるようにしました(1.0.0-56)
・(改善)メニューのフォルダと購読を、長押しして動かせるようにしました(1.0.0-57)
・(機能追加)AURA Plus をご利用いただけるようになりました。2 週間おためしいただけます(1.0.0-58)
・(改善)キーワード購読を、無料でも 3 件までお使いいただけるようにしました。4 件目からは AURA Plus が必要です(1.0.0-59)
・(機能追加)購読を追加するときに、記事の画像を表示するかどうかを選べるようにしました。あとから購読の管理でも変えられます(1.0.0-60)
・(機能追加)毎回入るロゴなど、不要な画像を隠せるようにしました。記事の画像を長押しして「この画像を今後表示しない」を選ぶと、その購読の記事で以後表示しません(1.0.0-61)
・(機能追加)記事の中の不要な部分をタップして、その購読で今後隠せるようにしました。記事の画面の右上のメニュー →「不要な部分を隠す」から使えます。隠したものは 設定 → 同期 →「隠している画像と部分」から戻せます(1.0.0-62)
・(改善)記事の本文の先頭に、表題と同じ見出しが重ねて出るときは自動で省くようにしました。省いたときは記事の画面にその旨を表示し、その場で戻せます(1.0.0-63)
・(改善)AURA Plus の使える・使えないの判定を 1 か所にまとめ、おためし中に眼鏡（Even G2）連携を含むすべての Plus の機能が使えることを確かめるようにしました(1.0.0-64)
・(改善)記事の不要な部分を隠すとき、複数の場所をまとめて選べるようにしました(1.0.0-65)
・(改善)複数のページに分かれた記事も、最後まで読めるようにしました。何ページ目かも分かります(1.0.0-66)
・(不具合修正)一部のサイト（ITmedia など）で記事の全文を取得できなかった問題を直しました。文字コードをページの中でだけ示しているサイトで、全文が文字化けして取得に失敗していました。キーワード購読で Yahoo!ニュースの記事の全文を取得できないことがあった問題も直しました(1.0.0-66)
・(機能追加)購読や設定をまとめて書き出し、別の端末で読み込めるようにしました。機種変更のときにお使いください(1.0.0-67)
・(不具合修正)設定を読み込んだあとに「眼鏡の合言葉は新しくなったので、入れ直してください」と出る問題を直しました。眼鏡への配信をオンにしている端末で、合言葉が変わっていないのに出ていました(1.0.0-68)
・(不具合修正)複数のページに分かれた記事で、途中のページを取得できなかったときに「全 N ページの記事をつなげて表示しています」と出ていた問題を直しました。いまは「N ページ目までを表示しています」と出します(1.0.0-69)
・(機能追加)記事のページに冒頭や抜粋しか無く「続きを読む」「もっとよむ」などで本文のページへ移る記事でも、本文を取得できるようにしました。別のページから取得したときは記事の画面にその旨を表示します(1.0.0-70)
・(改善)ページの番号だけが並ぶ形で分かれた記事も、最後まで読めるようにしました。あわせて、これまで冒頭や抜粋だけで取得していた記事の全文を、順に取り直します(1.0.0-71)
・(改善)記事の全文の取り直しを、Wi-Fi など通信量を気にしなくてよい回線のときだけ、公開から 3 日以内の記事に限って少しずつ行うようにしました。古い記事は記事の画面の「全文を取得」で取り直せます。1.0.0-71 で取り直しが始まっていなかった問題と、眼鏡の本文にページの区切りの行が入っていた点もあわせて直しました(1.0.0-72)
・(機能追加)記事の本文に埋め込まれた YouTube の動画を、サムネイルと再生の印で表示するようにしました。押したときに初めて読み込み、その場で再生できます。読み込めないときはリンクに切り替わり、通信できないときはその旨を表示します。設定 → 表示の「本文の埋め込みを表示する」をオフにするとリンクだけになります。これまで取得した記事は、Wi-Fi のときに直近の記事から順に取り直すと表示されます(1.0.0-73)
・(機能追加)記事の本文に埋め込まれた X と Instagram の投稿を枠で表示し、押すとその場で読み込めるようにしました。TikTok の投稿も枠で表示しますが、読み込めないときはリンクに切り替わります。設定の「本文の埋め込みを表示する」で YouTube と一緒に切り替えられます(1.0.0-74)
・(不具合修正)通信できないときに、更新できなかった配信元がすべて「配信元が見つかりません（サービス終了の可能性）」と表示されていた問題を直しました。端末の通信が止まっている（名前解決できない・つながらない）ときに起きていました。いまは「通信できません。ネットワークを確認してください」の 1 行にまとめ、「サービス終了の可能性」は配信元がページを返さない状態が 3 回続いたときだけ表示します。英語のエラーの文言もそのまま表示しないようにしました(1.0.0-75)
・(不具合修正)通信できないときに全文の先読みが記事を 1 件ずつ取りにいき、失敗として記録していたため、通信が戻っても 30 分ほどその記事を取得しなかった問題を直しました。いまは通信できない失敗は記録せず、続けて通信できなければその回の先読みと取り直しを止め、通信が戻った次の更新ですぐ取得します(1.0.0-76)
・(改善)以前のしくみで取得した記事の全文を、記事を開いたときに取り直すようにしました。Wi-Fi など通信量を気にしなくてよい回線のときだけ行い、取り直せないときや前より短くなるときは前の全文のまま表示します。同じ記事は一度試したら 1 日は取り直しません(1.0.0-77)
・(機能追加)全文の先読みが終わったときの表示に、その回の全文と画像の取得に使った通信量を出すようにしました。実際に受け取ったデータの量で、記事の中の動画を再生したときの通信量は含みません(1.0.0-78)
・(改善)全文を取得できなかったときに、デバッグの記録に同じ失敗が 2 行ずつ出ていたのを 1 行にまとめました(1.0.0-79)
・(不具合修正)YouTube の動画の記事に本文の取得をかけて毎回失敗し、起動のたびに取得をやり直していた問題を直しました。動画の視聴ページには本文が無いので取得の対象から外し、これまでに残っていた失敗の記録も消します。本文は配信元の説明文を使います(1.0.0-80)
・(改善)全文の抽出ルールを更新したときに、過去の失敗をすべて一度に取得しなおしていたのを、直近 3 日の記事に絞るようにしました。古い記事のために起動直後に大量の通信が走らなくなります(1.0.0-81)
・(不具合修正)全文の先読みが、更新の多い配信元に枠を取られて更新の少ない配信元の記事に順番が回らないことがあった問題を直しました。配信元をまたいで公平に、どの配信元も新しい記事から順に取得します(1.0.0-82)
・(改善)本文が取れているのに全文の判定が厳しすぎて捨てていた記事を、取得できるようにしました。短い速報記事や、見出しが煽り気味で本文と一致しにくい記事、価格に何度も触れる製品・セール記事などが対象です。ランキングや広告を本文と誤判定しないよう、実際の記事で基準を調整しています(1.0.0-83)
・(不具合修正)同じ記事を、画像ページ経由と記事ページ直で二重に取得し、一覧にも重複して表示することがあった問題を直しました。取得先が既にある記事と同じときは、重複として畳みます(1.0.0-84)
・(不具合修正)本文に埋め込まれた動画の再生中に、全画面のボタンを押しても全画面にならなかった問題を直しました。全画面で横向きに表示し、戻る操作で元に戻ります(1.0.0-85)
・(機能追加)YouTube チャンネルの購読で、記事にサムネイルの再生カードを表示し、押すとその場で動画を再生できるようにしました(1.0.0-86)
・(不具合修正)同じ記事が複数の配信元に入っているとき、その記事の本文を何度も取得していた問題を直しました。本文は一度だけ取得して同じ記事どうしで共有し、未取得の件数も記事の URL 単位で数えます(1.0.0-87)
・(改善)新着が無くても更新のたびに全文の先読みが始まっていたのを、前回の先読みから 15 分は空けるようにしました。手で更新したときはこれまでどおりすぐ始まります(1.0.0-88)
・(不具合修正)本文の取得先の乗り換えが、記事の中の関連リンクをたどって別の記事の本文に飛ぶことがあった問題を直しました。関連リンクの部品や、元と大きく異なるページはたどりません(1.0.0-89)
・(不具合修正)見出しと全く噛み合わない中身の無い塊を、まれに本文として取り込んでいた問題を直しました(1.0.0-90)
・(改善)動静・天気・発売のお知らせのように、本当に短くて完結している記事を「本文が短すぎる」で取りこぼしていたのを、取得できるようにしました(1.0.0-91)
・(不具合修正)埋め込み動画を全画面にした拍子に画面の向きが変わって再生が止まることがあった問題を直しました。回転や折りたたみで画面の幅が変わっても再生が続きます(1.0.0-92)
・(改善)先読みのあとの通信量の表示を「記事と画像の取得に使った通信量」に変えました（動画の再生は含みません）(1.0.0-93)
・(不具合修正)YouTube チャンネルの購読で、記事を開いても再生カードが出ないことがあった問題を直しました。以前に取り込んだ記事にもカードが出るようになり、「全文を取得」を押したときも、外部の取得はせずに埋め込みと説明文を組み直します(1.0.0-94)
・(改善)全文の先読みの進捗で、分母が常に上限（500）に見えていたのを、未取得の残り件数も併記するようにしました。1 回では取り切らないぶんは次の更新で続きから取得します(1.0.0-95)
・(不具合修正)記事の左右スワイプが、開いているフォルダ（タブ・購読）の外の記事まで移動してしまう問題を直しました。いま見ている一覧の並び順と絞り込みのとおりに前後へ動き、端で止まります(1.0.0-96)
・(改善)設定のタブを 7 つから 5 つ（購読 / 取得 / 表示 / 通知 / その他）に整理しました。説明文だけだった「クイックアクション」「閲覧」のタブをなくし、内容をそれぞれ「その他」「表示」に移しました(1.0.0-97)
・(機能追加)購読の追加・削除・名前の変更・フォルダ・並び替え・OPML を、設定の「購読」→「購読の管理」の 1 か所でまとめて行えるようにしました。購読の名前も後から変えられます(1.0.0-98)
・(改善)保存量の設定を 6 つから 3 つ（手元に残す量 / オフラインの深さ / 既読の自動整理）に整理しました。件数は「手元に残す量」から自動で決まり、いまの設定でおよそ何件・何日分が手元に残るかを実測値で表示します(1.0.0-99)
・(不具合修正)保持の上限で消えた記事を、記事の左右スワイプで開こうとして空白のページが出ることがあったのを直しました。消えた記事はスワイプの並びから自動で外れます(1.0.0-100)
・(不具合修正)題名が壊れている（空・極端に短い）記事で、中身の薄い本文を取り込んでしまうことがあったのを直しました。題名で本文の質を確かめられないときは取り込みません(1.0.0-101)
・(改善)取得タブの「いまの設定でおよそ何件が残るか」の表示から日数をやめ、件数と容量だけにしました。保存した記事があると日数が大きく伸びて分かりにくかったためです(1.0.0-102)
・(不具合修正)同じ記事が複数の配信元から入ったときの重複を、既読を書き換えずに畳むようにしました。「すべて」表示でも重複は 1 件にまとまり、以前に暫定で既読にしていた重複は未読に戻します(1.0.0-103)
・(不具合修正)共有から追加した購読の全文を、追加した直後に先読みするようにしました。ほかの先読みが動いていても取りこぼしません。YouTube の購読は記事と画像だけを取り込み、全文の取得はしません(1.0.0-104)
・(機能追加)裏で定期的に記事を集めるために電池の設定を「制限なし」にする案内を出すようにしました。設定はアプリからは変えられないので、端末の設定画面へご案内します。断ったあとはしばらく出しません(1.0.0-105)
・(機能追加)タブレットなどで、画像のキャッシュを SD カードに置けるようにしました。SD カードを抜いても記事の本文は端末本体に残るので消えません（画像だけがそのつど取り直しになります）(1.0.0-106)
・(機能追加)「RSS を追加」に海外の配信元を加えました。国を選ぶと、その国の主要なニュース・技術・生活の配信元から選べます。配信を実際に確認したものだけを収録しています(1.0.0-107)
・(機能追加)端末の既定言語と違う言語の記事を翻訳できるようにしました。端末内で翻訳する方式（AURA Plus・通信なし・言語ごとに初回だけモデルを取り込み）と、Chrome や端末の翻訳アプリに渡す方式を選べます(1.0.0-108)
・(改善)翻訳の動作（開始・結果・モデルの取り込みと削除・どの方式で動いたか）を、設定の Debug タブに記録するようにしました。翻訳がうまくいかないときの手がかりになります(1.0.0-109)
・(不具合修正)取り込み済みの翻訳モデルを削除できないことがあった問題を直しました。削除のあと本当に消えたかを確かめ、消せなかったときは理由をお伝えします。また、端末の言語のモデル（訳す先として常に必要）は、一覧に残したまま削除の対象から外しました(1.0.0-110)
・(改善)記事の翻訳を、端末の翻訳（Chrome や端末の翻訳アプリ）に渡す方式に一本化しました。端末内で翻訳する方式は訳の品質が実用に足りなかったため取り下げました。翻訳は無料でお使いいただけます。あわせてアプリの容量も小さくなりました(1.0.0-111)
・(不具合修正)海外の配信元を追加したあとのフォルダ選択で、新しいフォルダを作れないことがあった問題を直しました。フォルダが多いと作成欄が画面の外に隠れていました。国内の配信元を追加するときも同じように直しています(1.0.0-112)
・(機能追加)複数の端末で、設定・購読・フォルダ・フィルタ・隠すルール・保存した記事の一覧を同じにできるようになりました（設定 →「その他」→「端末間の同期」）。保存先はご自身の Google ドライブのアプリ専用フォルダで、提供元はデータを受け取りません。記事の本文と画像は同期しません。同期は任意で、サインインしなくても今までどおりお使いいただけます(1.0.0-113)
・(不具合修正)「今すぐ同期」やサインインの直後に同期が失敗することがあった問題を直しました。通信の処理が画面の担当につられて実行されていたのが原因です(1.0.0-114)
・(改善)端末間の同期の画面の説明を、提供元の名前（黒猫福家本舗）に統一しました(1.0.0-115)
・(機能追加)端末間の同期で、記事の既読・未読も揃うようにしました。片方で既読にした記事はもう片方でも既読に、未読に戻した記事はもう片方でも未読に戻ります。同じ記事で操作が食い違ったときは、あとから操作した方に揃えます。記事が多くても、変えたぶんだけを送ります(1.0.0-115)
・(不具合修正)保持できる件数を超えたときの記事の削除で、特定の配信元の記事だけがすべて消えることがあった問題を直しました。公開日ではなく取り込んだ順で古いものから消すようにし、新しく足した購読の記事が守られるようにしました。また、この削除で消した記事は目印を付けず、配信元にまだ載っていれば取り直せるようにしました(1.0.0-116)
・(不具合修正)端末間の同期で、フォルダや購読の並び順が揃わないことがあった問題を直しました(1.0.0-116)
・(改善)端末間の同期を AURA Plus の機能にしました。AURA Plus でないあいだは同期を止めますが、Google ドライブのデータには触れず、AURA Plus に戻ると自動で再開します。機種変更で購読・設定を移すだけなら、AURA Plus でなくても「機種変更（書き出しと読み込み）」でお使いいただけます(1.0.0-117)
・(機能追加)端末間の同期で「メインの端末」を引き継げるようにしました。新しい端末で「この端末をメインにする」を押すと、古い端末が手元になくてもメインを移せます。同じ項目が食い違ったときは、メインの端末の内容が優先されます(1.0.0-118)
・(不具合修正)購読が多いと、更新のたびに記事がすべて消えて入り直し、記事数が大きく増減することがあった問題を直しました。保存できる件数を超えて消した記事は、しばらく取り込み直さないようにしています(1.0.0-119)
・(改善)購読の数に対して「手元に残す量」が足りないと、取得の設定にご注意の表示が出るようにしました。更新のたびに古い記事が消えてしまう状態が分かります(1.0.0-120)
・(不具合修正)「義兄に」「初恋の人」のような短い題名の記事で、全文が取れないことがあった問題を直しました。題名が短くても本文そのもので判断するようにしました(1.0.0-121)
・(改善)AURA Plus の案内に「端末間の同期」を載せました(1.0.0-122)
・(不具合修正)端末間の同期で、以前に既読・未読にした記事の状態が、もう片方の端末に伝わらないことがあった問題を直しました。同期のたびに「どこまで同期したか」の起点が最新の時刻まで進み、それより前に付けた既読・未読が持ち寄られなくなっていたのが原因です。これからは既読・未読の記録がある記事を毎回持ち寄ります。なお、片方の端末にしか無い記事の既読は、次の「記事の一覧の同期」で揃うようになります(1.0.0-123)
・(機能追加)端末間の同期で、記事の一覧もそろうようになりました。片方の端末にしか無い記事も、題名・日付・配信元がもう片方に取り込まれ、記事の総数・未読数・保存数が 2 台で一致します。これにより、以前は片方にしか無くて伝わらなかった既読・未読もそろいます。保持できる件数を超えて片方で消した記事は、もう片方でも同じように消えます。本文と画像は容量が大きいため同期せず、各端末で取得します(1.0.0-124)
・(改善)設定の「その他」に、問い合わせ用のメールと SNS（YouTube・X・note）を並べました。押すとそれぞれのアプリ・ページが開きます(1.0.0-125)
・(不具合修正)端末間の同期が、通信の途中で止まって失敗することがあった問題を直しました。接続や応答を待つ時間の上限が短く、少しの停滞で同期全体が失敗していたため、待ち時間を広げました(1.0.0-126)
・(不具合修正)端末間の同期の途中で設定画面を離れる・ホームに戻る・画面を回転させると、同期が中断されることがあった問題を直しました。同期を画面と切り離し、最後まで実行するようにしました(1.0.0-127)
・(改善)端末間の同期の通信量を減らしました。記事の一覧を日付ごとの単位に分けて持ち寄り、変わった分だけを送るようにしたので、2 回目以降の同期がずっと軽くなります(1.0.0-128)
・(改善)端末間の同期の画面に、そろっている記事・未読・保存の数と、未同期の変更の有無を出すようにしました。同期中はどこまで進んだかが分かります。初回は配信元が多いと数分かかることがありますが、画面を閉じても同期は続き、途中で止まっても次に開いたときに続きから同期します。本文と画像は各端末で取得する旨も添えています(1.0.0-129)
・(改善)端末間の同期の画面に「同期のしくみ」の説明を足しました（押すと開きます）。何がそろって何がそろわないか、数字がどう見えるか、初回に時間がかかること、データの置き場所を、やさしい言葉でまとめています。同じ説明は[公式サイト](/sync)にもあり、プライバシーポリシーからも辿れます(1.0.0-130)
・(不具合修正)端末間の同期で、2 台の記事数が合わないことがあった問題を直しました。片方の端末で「手元に残す量」を超えて消した記事が、もう片方の端末では消えずに残り、記事数がずれていました（消す指示が片方向にしか伝わっていませんでした）。これからは両方で同じように消えて数がそろいます。なお、保存した記事は、もう片方で消えても残します(1.0.0-131)
・(改善)端末間の同期を速くしました。2 回目以降の同期にかかる時間を大きく縮めています（Google ドライブへの問い合わせ回数を減らし、まとめて並行して行い、前回から変わっていない分は取りに行かないようにしました）。同期する通信量は増えていません(1.0.0-132)
・(不具合修正)端末間の同期が、記事の数はそろっているのにいつまでも軽くならず、毎回たくさん通信していた問題を直しました。同じ記事でも題名が端末ごとにわずかに違うと、互いに自分の題名で上書きし合って延々とやり取りが続いていたのが原因です。これからは「どの記事があるか」だけで同期の要否を判断し、題名の違いでは通信しません。また、同期の最中に記事の取り込みが同時に走って食い違うことがあったのを、取り込みを同期のあとに回して整えました。※更新して最初の 1 回だけは作り直しのため時間がかかり、その後は軽くなります(1.0.0-133)
・(不具合修正)Even G2（スマートグラス）の設定を端末間の同期の対象から外しました。眼鏡は 1 台のスマホにしかつなげないため、同期で設定が伝わると相手の端末で合言葉が作り直されて衝突していました。眼鏡の設定は端末ごとに持つようにしました(1.0.0-133)
・(改善)記事一覧の一番上の見出し（「AURA」など）を押すと、一覧の先頭まで素早く戻れるようにしました。押せることが分かるよう、見出しの右に上向きの小さな記号を添えています。すべての一覧画面で使えます(1.0.0-133)
・(機能追加)端末間の同期に「同期をやり直す」を追加しました（設定 → その他 → 端末間の同期。メインの端末だけで使えます）。Google ドライブに保存している同期データをすべて消し、その端末の内容で作り直します。同期の形式を更新してきた過程で古いデータが残り、記事の数がうまくそろわないときにお使いください。ほかの端末は次に同期したときに作り直されたデータを読み込みます。この端末の記事・既読・保存した記事は消えません。作り直したあと最初の 1 回の同期は時間がかかります(1.0.0-134)
・(不具合修正)端末間の同期で、片方の端末が保持できる件数を超えて消した記事を、もう片方の端末（特に新しく同期し始めた端末）が配信元から全部取り直してしまい、いつまでも記事数がそろわない問題を直しました。これからは、相手が消した記事の目印を受け取った端末にも控え、その端末が同じ記事を取り直さないようにします（両端末が同じ「手元に残す量」にそろいます）。あわせて「同期をやり直す」が途中で止まっても、続きから自動でやり直して最後まで作り直すようにしました(1.0.0-135)
・(改善)「同期をやり直す」を、メインの端末の状態にほかの端末を完全に合わせる動作にしました。メインの端末で購読・フォルダ・設定・フィルタ・隠すルールを整えてから実行すると、ほかの端末が次の同期でそれにそろい、ほかの端末にしか無い購読は消えます（各端末で購読がばらついたときに揃え直せます）。保存した記事は、その購読が消えても残します。メインの端末だけで実行できます(1.0.0-137)
・(改善)端末間の同期の画面に出る「記事」の数を、一覧に実際に出ている記事の数と一致させました。これまではフィルタで隠している記事も数に含めていたため、画面の見た目と数字がずれることがありました(1.0.0-138)
・(改善)手元に残す量を超えた記事の扱いを見直しました。これまでは古い記事を実際に削除していましたが、削除はやめ、配信元ごとに新しい方から決まった件数だけを表示する形にしました（保存した記事は古くても残ります）。端末の時計に左右されず、同じ記事なら 2 台で同じ結果になります。取り込みを繰り返しても、いったん表示から外れた記事を取り直して記事数が乱高下することがなくなります(1.0.0-139)
・(改善)「配信元ごとに表示する件数」を、購読の数に左右されないようにしました。これまでは「手元に残す量」を購読の数で割って決めていたため、購読の増減が 2 台に伝わる途中で件数がずれ、同じ記事でも表示される集合が食い違うことがありました。これからは「手元に残す量」の設定だけから件数が決まり、2 台で必ず同じになります(1.0.0-140)
・(改善)端末間の同期のしくみを作り直しました。これまでは各端末が別々に取得・整理した結果をあとから突き合わせていたため、記事数が 2 台でそろわないことがありました。これからは「どの端末が何を見たか」を記録として持ち寄り、各端末が同じ記録から同じ規則で数え直すので、同じところまで同期した 2 台は記事数・未読数・保存数が必ずそろいます。更新後の最初の同期で自動で新しい方式に切り替わります。記事・既読・保存はそのまま引き継がれ、これまでのデータも残します。もし「アプリの更新が必要です」と出たら、片方の端末だけ新しい方式になっているので、両方を最新にしてください(1.0.0-141)
・(不具合修正)新しい端末や設定を消した端末で同期しても、購読・記事・保存が戻らないことがあった問題を直しました。同じ Google アカウントでサインインして同期すれば、購読・フォルダ・記事・既読・保存した記事がすべて元どおりに戻ります（機種変更でお使いください）(1.0.0-142)
・(不具合修正)サインアウトしても Google の認証が残り、別のアカウントでサインインし直せないことがあった問題を直しました。サインアウトすると認証も解除し、次のサインインでアカウントを選び直せます(1.0.0-143)
・(不具合修正)サインアウトのあと「サインイン」を押しても、アカウントの選択画面が出ずに同じアカウントで入ってしまう問題を直しました。前回のアカウントの記録も消すようにしたので、サインアウトのあとは別のアカウントを選び直せます（機種変更や複数アカウントでお使いください）(1.0.0-144)
・(不具合修正)記事を開くとアプリが落ちることがあった問題を直しました。記事の言語を判定するしくみが、配布用ビルドの最適化で正しく動かなくなっていたのが原因です。あわせて、万一落ちたときに原因の記録が残るようにし、次に開いたときに確認できるようにしました(1.0.0-145)
・(改善)サインインの開始・成功・失敗をログに残し、失敗したときは画面にも理由を出すようにしました。一部の端末でサインインできないときの原因を確認しやすくしました(1.0.0-146)
・(不具合修正)端末ごとに購読の数が違うとき、同期しても揃わない（少ない方に合ってしまう）ことがあった問題を直しました。これからは両方の購読を足し合わせて、どちらの端末も同じ購読になります(1.0.0-143)
・(改善)「手元に残す量」「通信（回線）」「オフラインの深さ」は、空き容量や回線が端末ごとに違うため、端末ごとの設定にしました（同期しません）。このため「手元に残す量」が端末で違うと、一覧に表示される記事の件数は端末どうしでそろわないことがあります（購読・既読・保存はそろいます）(1.0.0-147)
・(機能追加)一覧の表示の切り替えに「読み込み済み」を追加しました。本文を取得済みで、通信なしで読める記事だけが並びます(1.0.0-147)
・(改善)「更新間隔」の説明を実際の動きに合わせました。Android の省電力のしくみで、自動更新は指定した間隔ぴったりにはならず、遅れたりまとめて回ったりします（目安の間隔です）。すぐ更新したいときは一覧を下に引くか、⋮ の「更新」をお使いください(1.0.0-148)
・(不具合修正)新着の通知が通知領域に出ず、アプリアイコンのバッジも付かないことがあった問題を直しました。通知の種類（チャンネル）が低い重要度に落ちていると出なくなるため、正しい設定で作り直しました。もし出ないときは、端末の「設定 → 通知 → AURA → 新着」の重要度が「サイレント」より上になっているかをご確認ください(1.0.0-149)
・(機能追加)一覧の表示の切り替えに「未読（オフライン）」を追加しました。本文を取得済みで、まだ読んでいない記事だけが並びます。オフラインでこれから読むものを選ぶのに使えます(1.0.0-150)
・(改善)自動・手動の更新での本文の先読みを、その回の新着ぶんだけにしました。これまでは新着が数件でも未取得の記事をまとめて（最大 500 件）先読みしていたため、更新のたびに通信量と時間がかかっていました。新着 4 件なら通信量は数十分の一になります(1.0.0-150)
・(改善)本文の先読みの量を回線に合わせるようにしました。Wi-Fi のときは新着に加えて、まだ本文を持っていない記事も少しずつ先読みして手元に埋めます。従量制（モバイル）のときは新着ぶんだけにします。設定は増やさず、「取得」の通信・オフラインの深さから判断します(1.0.0-151)
・(機能追加)同期や OPML などで未取得の記事が一度に大量に増えたとき、「いま全部落とす／あとで少しずつ／落とさない」を選べるようにしました。Wi-Fi なら一気に、従量制なら控えめに、と選べます。ふだんの更新では出ず、大量に増えたときだけ確認します(1.0.0-151)
・(機能追加)設定 →「取得」に「未取得の記事をいま全部落とす」を追加しました。バックログを抱えているとき、思い立ったときに押せます。件数と見込みの通信量（従量制ならその注意も）を確かめてから、まとめて先読みします。上の確認で「落とさない」を選んだあとの解除も同じ場所にあります(1.0.0-151)
・(改善)画像を SD カードに移す・本体に戻す操作を、設定 →「取得」の「手元に残す量」の下にいつでも辿れるように置きました。これまでは SD カードを挿したときの案内でしか操作できませんでした。記事の本文は本体に残るので、SD カードを抜いても記事は消えません(1.0.0-152)
・(不具合修正)設定 →「取得」の「いまの設定で、およそ N 件が手元に残ります」の件数が、キャッシュが少ないときに小さすぎる値（例:「およそ 3 件」）になっていたのを直しました。いまの上限に入るおよその件数を出すようにし、手元の本文が少ないときは目安として出します(1.0.0-152)
・(不具合修正)記事一覧で「戻る」を押したときにアプリが落ちることがあったのを直しました。一覧（最上位の画面）での「戻る」は、これまでどおりアプリを閉じる動作になります(1.0.0-153)
・(改善)記事を読み終えて閉じたとき、一覧の先頭ではなく、開いた記事の位置に戻るようにしました。左右にスワイプして別の記事を読んだ場合は、最後に見ていた記事の位置に戻ります。タブやフォルダを切り替えたときは先頭に戻ります(1.0.0-154)
・(機能追加)記事一覧の右端に縦のスクロールバーを追加しました。つまんで動かすと一気に移動でき、動かしているあいだはその位置の記事の年月日が出るので、どのあたりの時期を見ているか分かります。色・太さ・透明度・形状は 設定 →「表示」→「スクロールバー」で変えられ、表示しないこともできます(1.0.0-154)
・(機能追加)クーポンコードに対応しました。お渡しした 6 文字のコードを 設定 →「購読」→「クーポンコード」に入力すると、その端末で一定期間だけ AURA Plus をお使いいただけます。いつまで使えるかは、同じ場所に日時と残りで表示します。期限を過ぎると、お知らせせずに Free へ戻ります（作ったフィルタなどの設定は消えずに残ります）(1.0.0-155)
・(不具合修正)「未読記事」「未読（オフライン）」で、読んで戻ったあと一覧にグレーで残っている記事をもう一度押すと「この記事は見つかりませんでした」になるのを直しました。一覧に見えている記事は必ず開けます。グレーで残っている記事へ、左右のスワイプでも移れます。検索や「隠した記事」から開いた記事でも同じことが起き得たので、あわせて直しました (1.0.0-156)
・(内部の改善)端末間の同期（AURA Plus）で、通信量を減らすための準備を入れました。同期の結果や件数は今までと変わりません。いまは、新しい運び方でも同じ結果になるかを同期のあとに確かめているだけです。確かめるのは Wi-Fi のときだけで、そのあいだは Wi-Fi の通信量が少し増えます (1.0.0-157)
・(内部の改善)端末間の同期（AURA Plus）の「新しい運び方の確かめ」を、途中でアプリを閉じても次の同期で続きから進むようにしました。これまでは、最初の確かめが終わる前にアプリを閉じると、次の回にまた最初からやり直していました。確かめの結果（一致・不一致の累計）は、設定 →「その他」→「端末間の同期」に 1 行で出ます。同期の結果や件数は今までと変わりません (1.0.0-158)
・(不具合修正)クーポンで AURA Plus をお使いの端末で、アプリを開いたときの端末間の同期が、ときどき行われないことがあったのを直しました。アプリを開いた直後の一瞬だけ Free と判定されていたのが原因です。手動の「今すぐ同期」は今までも動いていました (1.0.0-159)
・(内部の改善)端末間の同期（AURA Plus）の「新しい運び方の確かめ」で、ほかの端末のデータを読んでいる途中にアプリを閉じても、次の同期で続きから読むようにしました。これまでは、読み終える前に閉じると次の回にまた最初から読み直していました。同期の結果や件数は今までと変わりません (1.0.0-160)
・(不具合修正)既読の自動整理（AURA Plus）と端末間の同期を一緒に使っていると、片付けた記事が次の同期で戻ってきて、また片付けられる、を繰り返していたのを直しました。既読の自動整理は、記事を一覧から隠す方式に変わりました。読み終えてから選んだ日数が過ぎた記事は、今までどおり一覧・未読数・検索から消えます（履歴には残ります）。保存（★）した記事は消えません (1.0.0-161)
・(内部の改善)端末間の同期（AURA Plus）の「新しい運び方の確かめ」で、データの小分けの単位を大きくし、小分けの数を減らしました。この版にした最初の同期で、確かめ用のデータを一度だけ上げ直します（Wi-Fi のとき・途中でアプリを閉じても次の同期で続きから進みます）。あわせて、更新や同期のたびに端末の中の記事の通し番号が大きく飛んでいたのを直しました。同期の結果や件数は今までと変わりません (1.0.0-162)
・(不具合修正)端末間の同期（AURA Plus）で、2 台の端末がほぼ同時に同期すると、同期に参加している端末の一覧から片方が一時的に漏れ、その回はその端末の既読や保存が届かないことがあったのを直しました。初めて同期する端末が同時に 2 台あるときに起きていました (1.0.0-163)
・(内部の改善)端末間の同期（AURA Plus）の「新しい運び方の確かめ」で、データの小分けの単位を元の大きさに戻しました。1.0.0-162 で記事の通し番号が飛ばなくなったので、元の大きさの方が毎回の同期で運ぶ量が少なく済みます。同期の結果や件数は今までと変わりません (1.0.0-164)
・(不具合修正)端末間の同期（AURA Plus）で、ほかの端末から受け取った記事が題名だけで入り、開けなかったのを直しました。新しい端末に入れたとき・入れ直したとき・設定を「置き換えて読み込む」で読み込んだあとに起きていました。これからは元の記事へのリンクつきで入ります。すでに題名だけで入っている記事も、次の同期でリンクが入ります（ほかの端末がリンクを持っている記事に限ります）。本文は、先読みや記事を開いたときに取得します (1.0.0-165)
・(不具合修正)端末間の同期（AURA Plus）を使っていると、購読を解除しても次の同期で元に戻ってしまうのを直しました。解除した購読は、同期しているほかの端末からも消えます。解除したあとで、どれかの端末で同じ購読を登録し直せば、すべての端末に戻ります。同期している端末は、すべてこの版以降にそろえてお使いください（古い版の端末が混ざっていると、解除が取り消されることがあります） (1.0.0-166)
・(改善)購読を解除するときの確認に、一緒に消える記事の数と、そのうち保存（★）した記事の数を出すようにしました。保存した記事も購読と一緒に消えるためです。端末間の同期（AURA Plus）を使っているときは、ほかの端末からも消えることも出します (1.0.0-167)
・(改善)端末間の同期（AURA Plus）で受け取った記事のうち、どの端末にも元の記事へのリンクが無く、開けないものを一覧に出さないようにしました。記事そのものは残してあり、あとからリンクが入れば一覧に戻ります。保存（★）した記事は今までどおり出ます (1.0.0-168)
・(改善)まだ本文を持っていない記事が数千件あるとき、更新してからオフライン用の本文の取得が始まるまでに 10 秒以上かかっていたのを直しました。新しい端末に入れた直後や、端末間の同期で記事をたくさん受け取ったあとに起きていました。取得する順番は今までと同じです (1.0.0-169)
・(内部の改善)端末間の同期（AURA Plus）で、版の違う AURA が混ざっていても、新しい版が足した設定の項目を、古い版の端末が同期のときに消してしまわないようにしました。この版以降の AURA どうしで効きます (1.0.0-170)
・(内部の改善)端末間の同期（AURA Plus）で、保存した時刻の記録が無い古い保存記事があると、同期のたびにその記事の保存の情報を送り直していたのを直しました。保存した記事そのものは変わりません。あわせて、自動更新が途中で取りやめになったときに、内部の記録へ「失敗」と書いていたのを直しました (1.0.0-171)
・(不具合修正)端末間の同期（AURA Plus）が裏で動いている途中に、回線が切り替わるなどして取りやめになると、設定の「端末間の同期」に「同期できませんでした」と出ることがあったのを直しました。取りやめは失敗ではなく、次の同期で続きから進みます (1.0.0-172)
・(改善)設定の「端末間の同期」（AURA Plus）に、同期している端末の一覧を出すようにしました。端末ごとに、最後に同期した日と、現役か休止か（30 日以上同期していない端末）が分かります。休止の端末の記録は消えず、その端末で同期すれば現役に戻ります (1.0.0-174)
・(内部の改善)端末間の同期（AURA Plus）の通信量を減らす準備をしました。ほかの端末の記録を小分けで受け取る仕組みを入れましたが、この版ではまだ使わず、同期の動きは今までと同じです (1.0.0-175)
・(改善)端末間の同期（AURA Plus）で、ほかの端末の記録を小分けで受け取るようにしました。同期で使う通信が減ります。小分けの中身が少しでも合わないときは、これまでどおりの記録をそのまま読むので、購読・既読・保存の内容が変わることはありません (1.0.0-176)
・(内部の改善)次の機能のための内部の準備を入れました。この版では画面も動きも変わりません (1.0.0-177)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-178)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-179)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-180)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-181)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-182)
・(内部の改善)内部の準備の続きです。この版でも画面も動きも変わりません (1.0.0-183)
・(対応)連携の検証版（内部）です。配信はしていません (1.0.0-184)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-185)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-186)
・(改善)記事の一覧の絞り込みの名前と並びを整えました。「すべての記事 / 未読記事 / 未読記事（全文取得済）/ すべての記事（全文取得済）/ 保存した記事 / 閲覧履歴」になります。絞り込む条件は変わっていません (1.0.0-187)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-187)
・(機能追加)不具合のご報告に使えるログを、設定 →「その他」→「不具合の報告用のログ」に追加しました。オンにしたあいだだけ端末の中に記録が溜まり、ファイルに書き出したり共有したりできます。はじめはオフで、オフのあいだは記録しません。自動で送ることはありません (1.0.0-188)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-188)
・(不具合修正)キーワード購読で語をスペースで区切って入れたとき、すべての語を含む記事だけが出るようにしました。これまでは片方の語しか含まない記事が混ざっていました。登録済みの購読も自動で切り替わります（語と名前は変わりません） (1.0.0-189)
・(不具合修正)本文をあとから読み込むページで、「Loading...」だけを本文として保存してしまい、記事が空に見えることがあったのを直しました。読み込めていない本文は保存せず、配信元の要約を出したうえで次の機会に取り直します。すでに保存していたものも取り直しの対象に戻ります (1.0.0-189)
・(改善)記事の上に出ていた「N か所を隠しています」の帯を、はじめは出さないようにしました。隠し方はこれまでどおりで、隠したものの確認と取り消しは記事の右上の「⋮」から行えます。帯を出したいときは 設定 →「表示」→「隠したときに知らせる」をオンにしてください (1.0.0-189)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-189)
・(不具合修正)記事に出てくる日付を放送日として取り違えることがあったのを直しました。記事の中の古い年号（例: 1967年）や、ずっと先の日付を放送日として扱わないようにしています (1.0.0-190)
・(不具合修正)「（木曜・後8時半）」のように書かれた放送時刻を、午前8時半と取り違えていたのを直しました (1.0.0-190)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-190)
・(不具合修正)「10月1日スタート」のように、これから始まる放送を伝える記事を取りこぼしていたのを直しました。先の日付でも、番組の改編期にあたる範囲までは読み取るようにしています (1.0.0-191)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-191)
・(不具合修正)番組名に曜日が入っている番組（「月曜◯◯」「木曜◯◯」など）で、記事に書かれた別の日付（お知らせの配信時刻やチケットの発売日）を放送日として扱っていたのを直しました。番組名の曜日に合う直前の放送日で探します (1.0.0-192)
・(不具合修正)たくさんの局と番組を並べた記事で、その番組の局を取りこぼすことがあったのを直しました。番組名のすぐ前に書かれている局を先に見ます (1.0.0-192)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-192)
・(不具合修正)記事がたくさん溜まっている状態で、一覧の読み込み中に裏で更新が走るとアプリが終了することがあったのを直しました (1.0.0-193)
・(機能追加)設定 →「その他」→「不具合の報告用のログ」に「ログをコピーする」を追加しました。書き出しや共有がうまくいかない端末でも、貼り付けてお送りいただけます (1.0.0-193)
・(改善)溜まっているログの「件数」を表示し、書き出し・共有・コピーの結果を必ず画面に出すようにしました。ログが 0 件のときは、その場でお知らせします (1.0.0-193)
・(改善)同じ記事ページを短い間に何度も取りにいくことがあったのを直しました。取れなかった記録を、同じページの記事どうしで共有します (1.0.0-193)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-193)
・(機能追加)本文が JavaScript で組まれているページ（開いた直後に「Loading...」だけが見えるページ）からも、全文を取得できるようになりました。取得のときだけ裏でページを組み立てて読み取ります (1.0.0-194)
・(改善)同じ記事ページを、一度の取り込みの中で二重に取りにいかないようにしました (1.0.0-194)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-194)
・(不具合修正)ダークな表示で、確認の画面のボタン（「やめる」「コピーする」など）の文字が背景に没して読めなかったのを直しました。あわせてアプリ内の確認画面とメニューの色を、明るい表示・ダーク・ブラックのどれでも読める色に揃えました (1.0.0-195)
・(改善)購読の追加の入口を「購読を追加」1 つにまとめました。URL・配信元の一覧・海外の配信元・キーワードを、同じ画面の中で選べます (1.0.0-195)
・(機能追加)「購読を追加」に Google ニュースと Yahoo!ニュースの段を追加しました。主要・国内・国際・経済（ビジネス）・科学技術・エンタメ・スポーツなどを、一覧から 1 タップで購読できます (1.0.0-195)
・(改善)購読の管理をフォルダごとにまとめて表示し、行を長押ししてフォルダの見出しを越えると、そのフォルダへ移せるようにしました。長押しして動かさずに離すと、その購読の設定が開きます (1.0.0-195)
・(改善)未取得の本文を**最後まで取り切る**ようにしました。これまでは 1 回の更新で 50 件ずつしか進まず、未取得がなかなか減りませんでした (1.0.0-196)
・(改善)件数が多いときは、取得の前に「◯◯ 件の本文を取得します」とお聞きするようにしました。モバイル回線のときは件数にかかわらずお聞きし、自動更新では次の Wi-Fi まで見送ります (1.0.0-196)
・(改善)購読を追加したとき、取得中でも**待たずにその購読の本文を先に取る**ようにしました。これまでは走っている取得が終わるまで待たされていました (1.0.0-196)
・(改善)「購読を追加」の URL とキーワードを 1 つの画面にまとめ、海外の配信元を一番右へ移しました。配信元の一覧からは Yahoo!ニュースを外しました（専用の段と二重のため） (1.0.0-196)
・(改善)メニュー（三本線）でも、チャネルを長押しして**フォルダをまたいで移せる**ようにしました。長押しして動かさずに離すと、その購読の設定が開きます (1.0.0-196)
・(改善)一覧で見ているフォルダは、メニューを開いたときに開いた状態で出るようにしました (1.0.0-196)
・(対応)連携の検証版（内部）の続きです。配信はしていません (1.0.0-197)
・(改善)記事の本文に表があるとき、枠線と見出しを付けて読めるようにしました。列の多い表は、表だけを横に動かして見られます (1.0.0-198)
・(改善)記事に出てくる放送の日時を、番組名のいちばん近くに書かれたものから読むようにしました。記事の配信時刻や別の番組の時刻を取り違えることが減ります (1.0.0-199)
・(改善)更新中の帯に、いま読んでいる配信元や記事とフォルダ名、進み具合を出すようにしました。見せ方は設定の「表示」→「更新の進行表示」で 3 つから選べます (1.0.0-200)
・(改善)更新中の帯を、自分で更新したときは詳しく出し、8 秒後に細い線へ畳むようにしました。自動更新のときは細い線だけです。線を押すと詳しい表示に戻せます (1.0.0-201)
・(不具合修正)更新中の帯で「取り込み」「先読み」の文字が枠の下に寄って見えていたのを直しました。細い線の表示では、進み具合の数字を線と同じ行に収めて場所を取らないようにしました (1.0.0-202)
・(不具合修正)更新中の帯で、配信元のアイコンや番組名が行の下に寄って見えていたのを直しました。細い線の表示では、進み具合の数字を線の右に並べるようにしました (1.0.0-203)
・(改善)更新中の帯を長押しで開いたときは、細い線へ自動で畳まないようにしました。もう一度押すまで開いたままです（軽く押して開いたときは、これまでどおり 8 秒で細い線に戻ります） (1.0.0-204)
・(機能追加)新しいバージョンがあると、一覧の上に案内が出るようになりました。「更新」を押すと裏で落とし、終わったら「再起動」で入れ替わります。設定 →「その他」→「更新を確認」でいつでも確かめられます（Google Play から入れた場合だけです）(1.0.0-207)
・(不具合修正)モバイル回線で自分で更新したとき、「全文は取得しませんでした（Wi-Fi 接続時に取得します）」の案内が出た直後に消えて見えなかったのを直しました。本文を取りに行っていたわけではありません(1.0.0-207)
・(不具合修正)メニュー（3 本線）で購読を長押しして動かすと、畳んだフォルダを 1 つ越えたところで止まってしまっていたのを直しました。2 つ以上先のフォルダへも 1 回で動かせます(1.0.0-207)
・(不具合修正)メニュー（3 本線）を開いたまま「戻る」を押すと、アプリが閉じていたのを直しました。まずメニューが閉じます(1.0.0-207)
・(改善)同期の Google サインインに失敗したとき、英語のエラーをそのまま出さず、日本語の案内を出すようにしました(1.0.0-207)
・(不具合修正)画像を隠したときの案内（戻す場所）と、設定「その他」の「記事の操作」の説明が、実際の画面と違っていたのを直しました(1.0.0-207)</div>

## 1.0.0-20〜32（2026-09-13）

<div className="paste">・(機能追加)通信の使い方を、**フィードの更新 / 記事の全文 / 画像**のそれぞれで「モバイルも許可 / Wi-Fi のみ / 取得しない」から選べるようにしました。設定 → 同期 にあります
・(機能追加)設定 →「その他」から、使い方・更新履歴・プライバシーポリシーなど公式サイトの各ページを直接開けるようにしました
・(機能追加)すべて既読にするとき、**範囲を選べる**ようにしました。場所（いま見ているところだけ / すべてのフォルダ）と期間（すべて / 1日以上前 / 1週間以上前 / 1か月以上前）を選ぶたびに対象の件数を数え直してお見せします
・(機能追加)**新着があったときに通知**できるようにしました（設定 → 通知・はじめは切）。アプリのアイコンに未読の数字（バッジ）も付きます。アプリを開くと通知もバッジも消えます
・(機能追加)**ホーム画面のウィジェットを作り直しました**。表示する対象（すべて / フォルダ / 購読）を選べ、大きさを広げたぶんだけ件数が増え、縦にスクロールできます。行を押すとその記事が直接開き、更新ボタンで取り込みを走らせられます。色や不透明度も選べます
・(機能追加)**キャッシュを容量で管理**できるようにしました（設定 → 同期）。いま使っている量（本文 / 画像 / 合計）を表示し、上限を 128MB〜無制限から選べます。読み終えた記事のぶんを自動で削除する設定と、その場で削除する操作も用意しました。**保存した記事のキャッシュはどの経路でも消しません**
・(機能追加)**フォルダの並べ替え**をドラッグでできるようにしました（購読リストの管理 → ⋮ →「フォルダの管理」）。指を離した時点で保存され、一覧のタブの並びもそのまま変わります。「未分類」は常に先頭です
・(機能追加)眼鏡（Even G2）に出す一覧に、**オフラインで読めるかどうかの印**を付けました
・(改善)先読みの進み具合が「16 / 1000」のように実際と合わない分母で出ていたのを、**未取得の実数**に直しました。取るものが無いときは走りません
・(改善)未読で絞り込んでいるとき、**記事を開いて戻っても一覧からすぐ消えない**ようにしました。読み終えた記事はグレーでその場に残り、前後の記事との位置関係が保たれます。絞り込み直すのは、タブや購読の切り替え・更新・アプリの開き直しのときです
・(改善)ヘッダーのアプリ名の下に、名前の由来（Article Update & Reading Assistant）を1行表示するようにしました。頭文字の A U R A がアクセント色になります
・(改善)**新着の帯を「反映のスイッチ」に変えました**。裏で記事が入っても読んでいる一覧は動かず、溜まったぶんが「▲ 新着 N 件」として帯に出ます。押すと一覧に入って先頭へ戻ります。自分で更新したときや先頭にいるときは、帯を出さずにその場で入ります
・(改善)**購読リストの管理を作り直しました**。1件2行の見やすい並びにして、上に検索欄とフォルダの絞り込みを置き、行を押すと URL の確認・フォルダの変更・先頭へ移動・購読の解除ができるシートが出ます。フォルダの管理は別画面に、OPML の読み込み / 書き出しは ⋮ に移しました
・(改善)**本文を公開していない記事（有料記事など）を全文取得の対象から外しました**。取れないページを何度も取りにいかなくなります
・(不具合修正)記事の本文の前後に、ポイント案内・SNS ボタン・見出しの重複・フッタ・関連ニュースの見出し一覧が混ざることがあった問題を修正
・(不具合修正)プル更新の輪（くるくる）が、記事のサムネイルの上に長いあいだ重なり続けていた問題を修正。取り込みが終わった時点で消えるようにしました
・(不具合修正)一覧のサムネイルが記事の写真ではなく**媒体のロゴ**になることがあった問題を修正。あわせて、本文の前後に見出しの重複・SNS ボタン・日付・関連ニュースが付いていた配信元（日テレなど）の本文を、記事だけ取り出せるようにしました
・(不具合修正)**同じ記事を何度も取りにいっていた問題を修正**。混雑やタイムアウトで取れなかった記事を覚えておき、しばらく置いてから取り直すようにしました。更新のたびに同じ記事で時間と通信を使うことがなくなります</div>

## 1.0.0-19（2026-09-11）

<div className="paste">・(不具合修正)**文字化けを直した**。配信元のデータに 1 バイトでも異物が混ざると、日本語がまるごと
  `æ–‡å­—åŒ–` のように化けることがあった。Shift_JIS 配信の取り違えも直した
・(機能追加)設定 → その他に **「文字化けした記事を直す」** を追加。すでに化けて入っている記事を読み直して戻す
  （押すとまず件数を数え、もう一度押すと直す）</div>

## 1.0.0-18（2026-09-11）

<div className="paste">・(不具合修正)**消した記事が戻ってくるのを止めた**。保持ポリシーで消した記事が次の取り込みで入り直し、
  読み終えた記事が未読に戻っていた</div>

## 1.0.0-17（2026-09-11）

<div className="paste">・(機能追加)**`http://` の配信元を読めるようにした**。OPML で持ち込んだ古い購読が軒並み取得エラーになっていた。
  まず `https://` を試し、対応していない配信元だけ `http://` になる
・(改善)取得エラーの文言を分かりやすくした（「配信元が見つかりません（サービス終了の可能性）」など）
・(機能追加)**取得エラーの購読だけを絞り込み、まとめて解除**できるようにした</div>

## 1.0.0-16（2026-09-11）

<div className="paste">・(機能追加)**[Even G2 連携](/g2) を追加**。眼鏡に記事を出せるようになった（設定 → 同期 → Even G2）</div>

## 1.0.0-15

<div className="paste">・(不具合修正)開いていない記事が既読になってしまう不具合を直した
・(不具合修正)全文取得の失敗をほぼ解消した（混雑時の待ち直し、通信方式の切り替え）</div>

## 1.0.0-14

<div className="paste">・(不具合修正)全文抽出に検証を入れ、広告や関連記事を本文として出さないようにした
・(機能追加)Google ニュースの中継 URL を解いて、キーワード購読でも全文を取れるようにした
・(機能追加)設定に **Debug** タブを追加</div>

## 1.0.0-13

<div className="paste">・(機能追加)スワイプ操作（右で保存 / 左で既読）、検索の絞り込みと履歴、オフラインの印
・(機能追加)端末のダークモード追従、文字サイズのスライダ、読み進み率のバー
・(機能追加)ホーム画面ウィジェット（未読合計と最新 3 件）、OPML の自動バックアップ
・(機能追加)タブレットで一覧と記事の 2 ペイン表示
・(機能追加)内蔵の配信元一覧から購読を追加できるようにした</div>

## 1.0.0-12 以前

<div className="paste">・(機能追加)リストモード 3 種（雑誌 / 大きなカード / リスト）、カスタムカラー、2 段タブ
・(機能追加)オフラインキャッシュの進捗表示、フォルダの作成と並び替え
・(機能追加)端末内検索、オンボーディング、OPML の入出力</div>

<!--
書き方のメモ:
- 1.0.0-33 からは 1変更＝1番号。コードを変えたコミットで versionCode を1つ上げ、
  同じコミットでこのファイルに1行足す（片方だけのコミットを作らない）。
- 配信していないぶんは「次回配信予定」に置き、配信したらその中身を
  `## 1.0.0 33-38（2026/9/xx 配信）` の見出しブロックへ移す。
- 利用者に見えない内部の変更（リファクタや検証まわり）は載せない。versionCode も上げない。
- 32 までは旧方式。遡って番号を振り直さない。
- 行はブロックごとに `<div className="paste">・(区分)…(1.0.0-NN)` 〜 `…</div>` で包む（らじぽけの docs と同じ）。
  「・」は Markdown のリスト記号ではないので、包まないと 1 つの段落に潰れる。1 行 1 項目、div の中に説明文を入れない。
-->
